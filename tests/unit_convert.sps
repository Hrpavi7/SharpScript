function main(void)
{
  system.output(system.convert(1, "km", "m"));
  system.output(system.convert(1, "m", "km"));
  system.output(system.convert(1, "m", "mi"));
  system.output(system.convert(1, "kg", "lb"));
  system.output(system.convert(32, "F", "C"));
  system.output(system.convert(0, "C", "F"));
  system.output(system.convert(0, "C", "K"));
}
